= render(partial: "layouts/title_panel", locals: {title: @paragraph.title(100)})

div class="p-6 my-5 bg-white rounded-sm"
  p class="text-lg" Edit
  = render partial: 'form', locals: {paragraph: @paragraph}